# Styx Registry Environment

# Domain for Caddy (e.g., registry.styx.sh)
DOMAIN=registry.styx.sh

# GitHub webhook secret (generate with: openssl rand -hex 32)
WEBHOOK_SECRET=your-webhook-secret-here
